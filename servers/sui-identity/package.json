{
  "name": "@mcpxgg/sui-identity",
  "version": "0.1.0",
  "private": true,
  "description": "Anchor MCP server #5 — zkLogin-aware identity resolver (SuiNS + reputation). Real resolvers in prod, hermetic in CI.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'sui-identity: lint placeholder'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@mcpxgg/server": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22",
    "tsx": "^4.19.0"
  }
}
