{
  "name": "@mcpxgg/cli",
  "version": "0.1.0",
  "private": false,
  "license": "Apache-2.0",
  "description": "mcpxgg CLI — npx mcpxgg publish, validate, init. Wired in Sprint 5.",
  "bin": {
    "mcpxgg": "./bin/mcpxgg.js"
  },
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo 'cli: tests added in Sprint 5'"
  },
  "dependencies": {
    "@mcpxgg/chain": "workspace:*",
    "@mcpxgg/shared": "workspace:*",
    "@mcpxgg/walrus": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22"
  }
}
