{
  "name": "mcpx-mcp-server",
  "version": "1.0.0",
  "description": "A starter MCP server template for the MCPX platform",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx scripts/dev.ts",
    "test": "tsx scripts/test.ts",
    "build": "tsc",
    "generate-config": "tsx scripts/generate-config.ts",
    "validate": "npx mcpxgg validate",
    "publish": "npx mcpxgg publish"
  },
  "dependencies": {
    "@mcpxgg/server": "workspace:*",
    "express": "^4.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
