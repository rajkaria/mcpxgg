{
  "name": "@mcpxgg/walrus",
  "version": "0.1.0",
  "private": true,
  "description": "Walrus + Seal client wrapper. HTTP publisher/aggregator with an in-memory backend for tests and local dev.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'walrus: lint placeholder'",
    "test": "node --import tsx --test 'src/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22",
    "tsx": "^4.19.0"
  }
}
