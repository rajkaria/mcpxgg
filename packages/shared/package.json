{
  "name": "@mcpxgg/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, errors, supabase clients, redis cache, twilio, validation across all apps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types/database": "./src/types/database.ts",
    "./validation/config-schema": "./src/validation/config-schema.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'shared: lint placeholder'",
    "test": "node --import tsx --test 'src/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@supabase/ssr": "^0.9.0",
    "@supabase/supabase-js": "^2.99.2",
    "@upstash/redis": "^1.37.0",
    "twilio": "^5.13.0"
  },
  "optionalDependencies": {
    "@sentry/node": "^8.40.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
