{
  "name": "@mcpxgg/chain",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Chain abstraction layer (CAL): ChainAdapter interface + per-chain adapters. Sui today, Base/Solana via adapters later.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./sui": "./src/sui-adapter.ts"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22"
  }
}
