{
  "name": "@mcpxgg/server",
  "version": "0.2.0",
  "private": false,
  "license": "Apache-2.0",
  "description": "Server SDK for building MCP servers that integrate with mcpxgg.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'sdk-server: lint placeholder'",
    "test": "node --import tsx --test 'src/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@mcpxgg/chain": "workspace:*",
    "@mcpxgg/x402": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22",
    "tsx": "^4.19.0"
  }
}
