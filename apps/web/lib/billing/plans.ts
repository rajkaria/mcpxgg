/**
 * Deprecation stub. See `./credits.ts` for the full context — Sprint 1's
 * chain rebuild eliminated the plan/credit/Razorpay model. Pricing is per
 * tool now (`priceAtomic: bigint` on the on-chain Server object).
 *
 * Sprint 3 (S3-T01..T11) deletes the whole `lib/gateway/*` tree and this
 * stub with it.
 */

export const PLANS = {} as Record<string, never>;
