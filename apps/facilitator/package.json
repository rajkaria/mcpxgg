{
  "name": "@mcpxgg/facilitator",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Sui x402 facilitator — open-source canonical implementation.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./app": "./src/app.ts",
    "./env": "./src/env.ts",
    "./gas-station": "./src/gas-station.ts",
    "./testing": "./src/sui/in-memory-backend.ts"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'facilitator: lint placeholder'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@mcpxgg/chain": "workspace:*",
    "@mcpxgg/x402": "workspace:*",
    "@mysten/sui": "^1.20.0",
    "hono": "^4.6.0",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
